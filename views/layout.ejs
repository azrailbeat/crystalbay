<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% if (typeof title !== 'undefined') { %><%= title %><% } else { %>Crystal Bay Tours<% } %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        /* Apple-like styling with light theme */
        :root {
            --primary-color: #0071e3;
            --secondary-color: #86868b;
            --background-color: #f5f5f7;
            --card-background: #ffffff;
            --success-color: #34c759;
            --danger-color: #ff3b30;
            --warning-color: #ff9500;
            --info-color: #5ac8fa;
            --border-radius: 10px;
            --text-color: #1d1d1f;
            --light-border: #e6e6e6;
            --light-accent: #f2f2f2;
            --sidebar-bg: #f4f4f5;
            --sidebar-active: #e1f0ff;
            --card-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            line-height: 1.6;
            background-color: var(--background-color);
            color: var(--text-color);
        }
        
        /* Main layout */
        .app-container {
            display: flex;
            min-height: 100vh;
        }
        
        /* Sidebar styling */
        .sidebar {
            width: 250px;
            background-color: var(--sidebar-bg);
            border-right: 1px solid var(--light-border);
            position: fixed;
            height: 100vh;
            z-index: 1000;
            overflow-y: auto;
        }
        
        .sidebar-logo {
            padding: 1.5rem;
            display: flex;
            align-items: center;
            border-bottom: 1px solid var(--light-border);
        }
        
        .sidebar-logo i {
            color: var(--primary-color);
            font-size: 1.5rem;
            margin-right: 0.75rem;
        }
        
        .sidebar-menu {
            padding: 1rem 0;
        }
        
        .sidebar-heading {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--secondary-color);
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            margin-top: 1rem;
        }
        
        .sidebar-item {
            display: flex;
            align-items: center;
            padding: 0.75rem 1.5rem;
            color: var(--text-color);
            text-decoration: none;
            border-left: 3px solid transparent;
            transition: all 0.2s ease;
        }
        
        .sidebar-item:hover {
            background-color: var(--light-accent);
            color: var(--primary-color);
        }
        
        .sidebar-item.active {
            background-color: var(--sidebar-active);
            border-left-color: var(--primary-color);
            color: var(--primary-color);
        }
        
        .sidebar-item i {
            margin-right: 0.75rem;
            font-size: 1.1rem;
            width: 20px;
            text-align: center;
        }
        
        /* Content area */
        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 2rem;
        }
        
        /* Card styling */
        .card {
            border-radius: var(--border-radius);
            box-shadow: var(--card-shadow);
            border: 1px solid var(--light-border);
            background-color: var(--card-background);
            margin-bottom: 20px;
            overflow: hidden;
        }
        
        .card-header {
            background-color: var(--card-background);
            border-bottom: 1px solid var(--light-border);
            padding: 1rem 1.25rem;
            font-weight: 600;
        }
        
        /* Button styling */
        .btn {
            border-radius: var(--border-radius);
            padding: 0.5rem 1.2rem;
            transition: all 0.2s ease;
            font-weight: 500;
        }
        
        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        .btn-primary:hover {
            background-color: #0066cc;
            border-color: #0066cc;
        }
        
        .btn-outline-primary {
            color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        .btn-outline-primary:hover {
            background-color: var(--primary-color);
            color: white;
        }
        
        .btn-success {
            background-color: var(--success-color);
            border-color: var(--success-color);
        }
        
        /* Status badges */
        .badge {
            padding: 0.4em 0.8em;
            border-radius: 20px;
            font-weight: 500;
            font-size: 0.75rem;
        }
        
        .badge-primary {
            background-color: var(--primary-color);
        }
        
        .badge-success {
            background-color: var(--success-color);
        }
        
        .badge-warning {
            background-color: var(--warning-color);
        }
        
        .badge-danger {
            background-color: var(--danger-color);
        }
        
        /* Form controls */
        .form-control, .form-select {
            border-radius: var(--border-radius);
            background-color: white;
            border: 1px solid var(--light-border);
            color: var(--text-color);
            padding: 0.75rem 1rem;
            transition: all 0.2s ease;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 2px rgba(0, 113, 227, 0.15);
        }
        
        /* Top navigation */
        .top-navbar {
            background-color: var(--card-background);
            box-shadow: var(--card-shadow);
            border-bottom: 1px solid var(--light-border);
            padding: 0.75rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .top-navbar .nav-link {
            color: var(--text-color);
            margin-left: 1rem;
            padding: 0.5rem 0.75rem;
            border-radius: var(--border-radius);
        }
        
        .top-navbar .nav-link:hover {
            background-color: var(--light-accent);
        }
        
        /* Stats cards on dashboard */
        .stats-card {
            padding: 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .stats-icon {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: rgba(0, 113, 227, 0.1);
            color: var(--primary-color);
            font-size: 1.25rem;
        }
        
        .stats-info h3 {
            font-size: 1.75rem;
            font-weight: 600;
            margin-bottom: 0.25rem;
        }
        
        .stats-info p {
            color: var(--secondary-color);
            margin: 0;
        }
        
        /* Table styling */
        .table {
            border-radius: var(--border-radius);
            overflow: hidden;
        }
        
        .table thead th {
            background-color: var(--light-accent);
            border-bottom: 1px solid var(--light-border);
            font-weight: 600;
            color: var(--secondary-color);
        }
        
        .table tbody tr:hover {
            background-color: rgba(0, 113, 227, 0.03);
        }
        
        /* Kanban board styling for lead management */
        .kanban-board {
            display: flex;
            gap: 1rem;
            overflow-x: auto;
            padding-bottom: 1rem;
        }
        
        .kanban-column {
            min-width: 280px;
            max-width: 280px;
            background-color: var(--light-accent);
            border-radius: var(--border-radius);
            padding: 1rem;
        }
        
        .kanban-column-header {
            font-weight: 600;
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .kanban-card {
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--card-shadow);
            padding: 1rem;
            margin-bottom: 0.75rem;
            border-left: 3px solid var(--primary-color);
        }
        
        /* Loading spinner */
        .loading-spinner {
            width: 2.5rem;
            height: 2.5rem;
            border: 3px solid rgba(0, 113, 227, 0.1);
            border-radius: 50%;
            border-top-color: var(--primary-color);
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        /* Charts container */
        .chart-container {
            height: 300px;
            position: relative;
        }
        
        /* Footer styling */
        footer {
            background-color: var(--card-background);
            border-top: 1px solid var(--light-border);
            color: var(--secondary-color);
            padding: 1.5rem 0;
            margin-top: 2rem;
        }
        
        /* Utility classes */
        .fade-in {
            animation: fadeIn 0.5s ease-in-out;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        .text-primary { color: var(--primary-color) !important; }
        .text-success { color: var(--success-color) !important; }
        .text-warning { color: var(--warning-color) !important; }
        .text-danger { color: var(--danger-color) !important; }
        .text-info { color: var(--info-color) !important; }
        
        .bg-light { background-color: var(--light-accent) !important; }
    </style>
    <% if (typeof head !== 'undefined') { %><%- head %><% } %>
</head>
<body>
    <!-- App Container with Sidebar and Main Content -->
    <div class="app-container">
        <!-- Sidebar Navigation -->
        <aside class="sidebar">
            <div class="sidebar-logo">
                <i class="bi bi-tsunami"></i>
                <span>Crystal Bay Tours</span>
            </div>
            
            <div class="sidebar-menu">
                <div class="sidebar-heading">ОСНОВНОЕ</div>
                <a href="/dashboard" class="sidebar-item <% if (active_page === 'dashboard' || active_page === 'home') { %>active<% } %>">
                    <i class="bi bi-grid-1x2-fill"></i> Дашборд
                </a>
                <a href="/leads" class="sidebar-item <% if (active_page === 'leads') { %>active<% } %>">
                    <i class="bi bi-people-fill"></i> Обращения клиентов
                </a>
                <a href="/tours-search" class="sidebar-item <% if (active_page === 'tours' || active_page === 'tours-search') { %>active<% } %>">
                    <i class="bi bi-airplane-fill"></i> Поиск туров
                </a>
                <a href="/bookings" class="sidebar-item <% if (active_page === 'bookings') { %>active<% } %>">
                    <i class="bi bi-calendar-check-fill"></i> Бронирования
                </a>
                
                <div class="sidebar-heading">УПРАВЛЕНИЕ</div>
                <a href="/agents" class="sidebar-item <% if (active_page === 'agents') { %>active<% } %>">
                    <i class="bi bi-person-badge-fill"></i> Менеджеры
                </a>
                <a href="/analytics" class="sidebar-item <% if (active_page === 'analytics') { %>active<% } %>">
                    <i class="bi bi-bar-chart-fill"></i> Аналитика
                </a>
                <a href="/history" class="sidebar-item <% if (active_page === 'history') { %>active<% } %>">
                    <i class="bi bi-clock-history"></i> История
                </a>
                
                <div class="sidebar-heading">ИНТЕГРАЦИИ</div>
                <a href="/samo-testing" class="sidebar-item <% if (active_page === 'samo-testing' || active_page === 'samo-api') { %>active<% } %>">
                    <i class="bi bi-cloud-arrow-up-fill"></i> SAMO API & Тестирование
                </a>
                <a href="/messages" class="sidebar-item <% if (active_page === 'messages') { %>active<% } %>">
                    <i class="bi bi-chat-dots-fill"></i> Мессенджеры
                </a>
                
                <div class="sidebar-heading">СИСТЕМА</div>
                <a href="/agents-ai" class="sidebar-item <% if (active_page === 'agents-ai') { %>active<% } %>">
                    <i class="bi bi-cpu"></i> AI-агенты
                </a>
                <a href="/unified-settings" class="sidebar-item <% if (active_page === 'settings' || active_page === 'integrations') { %>active<% } %>">
                    <i class="bi bi-gear-fill"></i> Настройки
                </a>
            </div>
        </aside>
        
        <!-- Main Content Area -->
        <div class="main-content">
            <!-- Top Navigation Bar -->
            <div class="top-navbar">
                <div>
                    <h4 class="mb-0"><% if (typeof page_title !== 'undefined') { %><%= page_title %><% } else { %>Crystal Bay Tours<% } %></h4>
                </div>
                <div class="d-flex align-items-center">
                    <a href="#" class="nav-link">
                        <i class="bi bi-bell"></i>
                    </a>
                    <a href="#" class="nav-link">
                        <i class="bi bi-person-circle"></i>
                    </a>
                </div>
            </div>
            
            <!-- Page Content -->
            <div class="py-4">
                <%- body %>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="border-top">
        <div class="container-fluid px-4">
            <div class="row">
                <div class="col-md-4">
                    <h5>Crystal Bay Tours</h5>
                    <p class="text-secondary">Простая и удобная система бронирования туров с интеграцией SAMO API</p>
                </div>
                <div class="col-md-4">
                    <h5>Полезные ссылки</h5>
                    <ul class="list-unstyled">
                        <li><a href="/tours" class="text-secondary">Поиск туров</a></li>
                        <li><a href="/bookings" class="text-secondary">Управление бронированиями</a></li>
                        <li><a href="/messages" class="text-secondary">Сообщения</a></li>
                        <li><a href="/help" class="text-secondary">Помощь</a></li>
                    </ul>
                </div>
                <div class="col-md-4 text-md-end">
                    <h5>Контакты</h5>
                    <p class="mb-0 text-secondary">Email: info@crystalbay.travel</p>
                    <p class="text-secondary">Телефон: +7 (XXX) XXX-XX-XX</p>
                    <div class="d-flex justify-content-md-end mt-3">
                        <a href="#" class="text-primary me-3"><i class="bi bi-telegram"></i></a>
                        <a href="#" class="text-primary me-3"><i class="bi bi-envelope"></i></a>
                        <a href="#" class="text-primary"><i class="bi bi-whatsapp"></i></a>
                    </div>
                </div>
            </div>
            <hr class="my-4 border-secondary">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0 text-secondary">© 2025 Crystal Bay Tours. Все права защищены.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0 text-secondary">Создано с <i class="bi bi-heart-fill text-danger"></i> для путешественников</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Common JavaScript -->
    <script>
        // Helper function for showing toast notifications
        function showToast(message, type = 'info') {
            const toastContainer = document.getElementById('toast-container');
            if (!toastContainer) {
                // Create toast container if it doesn't exist
                const container = document.createElement('div');
                container.id = 'toast-container';
                container.className = 'position-fixed bottom-0 end-0 p-3';
                container.style.zIndex = '5';
                document.body.appendChild(container);
            }
            
            // Create the toast
            const toastId = 'toast-' + Date.now();
            const toast = document.createElement('div');
            toast.className = `toast align-items-center text-white bg-${type} border-0`;
            toast.id = toastId;
            toast.setAttribute('role', 'alert');
            toast.setAttribute('aria-live', 'assertive');
            toast.setAttribute('aria-atomic', 'true');
            
            // Toast content
            toast.innerHTML = `
                <div class="d-flex">
                    <div class="toast-body">
                        ${message}
                    </div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                </div>
            `;
            
            // Add toast to container
            document.getElementById('toast-container').appendChild(toast);
            
            // Show toast
            const toastElement = new bootstrap.Toast(document.getElementById(toastId), {
                autohide: true,
                delay: 5000
            });
            toastElement.show();
        }
        
        // Helper for API calls
        async function apiCall(url, method = 'GET', data = null) {
            try {
                const options = {
                    method,
                    headers: {
                        'Content-Type': 'application/json'
                    }
                };
                
                if (data && (method === 'POST' || method === 'PUT' || method === 'PATCH')) {
                    options.body = JSON.stringify(data);
                }
                
                const response = await fetch(url, options);
                return await response.json();
            } catch (error) {
                console.error('API call error:', error);
                showToast('Произошла ошибка при обращении к серверу', 'danger');
                return { success: false, error: error.message };
            }
        }
    </script>
    
    <!-- Wazzup Chat Component -->
    <script src="/js/wazzup-chat.js"></script>
    
    <% if (typeof scripts !== 'undefined') { %><%- scripts %><% } %>
</body>
</html>